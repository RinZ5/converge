# Individual data-structure diagrams

Each .puml file renders one title-free diagram for one record-like data
structure. The files contain no notes or links to other diagrams; referenced
types such as Role, WeeklySlot, and Option<int> are labels only. Render every
file separately to obtain one image per structure.

The numbered files follow the order used in Section 3.2 of chapter3.tex.
