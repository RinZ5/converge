info:
  name: post_branch
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:8080/api/branches
  body:
    type: json
    data: |-
      {
          "name": "Riverside",
          "capacity": 20
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
