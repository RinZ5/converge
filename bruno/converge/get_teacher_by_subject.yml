info:
  name: get_teacher_by_subject
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/teachers?subject_id=1
  params:
    - name: subject_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
